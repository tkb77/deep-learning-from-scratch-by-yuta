memo

```
cd ~/.ssh
ssh-keygen -t rsa
~/.ssh/id_rsa.pub
cat ~/.ssh/id_rsa.pub
ssh -T git@github.com

mkdir workspace
cd workspace/
git clone git@github.com:tkb77/deep-learning-from-scratch-by-yuta.git
git clone git@github.com:tkb77/deep-learning-from-scratch.git
git config --global user.name 
git config --global user.email 
git config --list

wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh 
conda
source /home/ubuntu/.bashrc
conda

df
df -h
https://qiita.com/mangano-ito/items/629b10ea5d1ab80f2cc6

conda install numpy
conda install matplotlib

date
sudo cp -p /usr/share/zoneinfo/Japan /etc/localtime

git add two_layer_net.py 
git commit -m "add epoc"
git push
```
